// import 'package:http/http.dart' as http; //we only use in dart

void mian() async {
  // only used in the dart pad
  // pass the url and path
  // var url = Uri.https('example.com', 'whatsit/create');
  // http.get(url);
}
